<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">

    <item android:id="@+id/menuSearch"
        android:title="Search"
        android:icon="@android:drawable/ic_menu_search"
        app:actionViewClass="android.widget.SearchView"
        app:showAsAction="always"
        ></item>

    <item android:id="@+id/action_settings"
        android:title="@string/action_settings"
        android:orderInCategory="100"
        app:showAsAction="never"/>

    <item android:id="@+id/action_mapType"
        android:title="Change Map Type"
        android:orderInCategory="100"
        app:showAsAction="never"/>

    <item android:id="@+id/show_notification"
        android:title="Show Notification(Demo Only)"
        android:orderInCategory="100"
        app:showAsAction="never"/>

    <item android:id="@+id/show_DBinfo"
        android:title="Delete DB"
        android:orderInCategory="100"
        app:showAsAction="never"/>

    <item android:id="@+id/getCount"
        android:title="Get Count"
        android:orderInCategory="100"
        app:showAsAction="never"/>

</menu>